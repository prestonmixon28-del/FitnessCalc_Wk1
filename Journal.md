Journal

Phase-1:
I put it at the bottom of the body so the HTML loads before the JavaScript runs. This will also connect the HTML page to the seperate script.js file.
Phase-2: 
I used numbers for steps and calories  per every step. I then multiplied the two numbers together tp calculate the calories burned.